# Zymatica Engine - Local Deployment Environment Variables
# Copy this file to '.env' and populate the values before running docker-compose.

# API authorization key for serving endpoints (e.g. /v1/chat/completions)
# Generate a secure key (e.g., openssl rand -hex 24)
ZYMATICA_API_KEY=your-secure-production-api-key-here
